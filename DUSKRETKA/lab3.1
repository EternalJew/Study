#include <iostream>
#include <conio.h>
#include <stdlib.h>
using namespace std;
int main()
{
	
	cout << " |" <<   " a " << "b " << "c "  << "   |" << " && ";
	cout << " && " << " && " << " || " << "&&"  << " |\n";
	cout << " --------------------------------\n";
	int a, b, c, d, f1, f2, f3, f4, f5, f6, f7, f8, f9, f10;
	for (int a = 0; a <= 1; a++) {
		for (int b = 0; b <= 1; b++) {
			for (int c = 0; c <= 1; c++) {
				
					f1 = !a && !b;
					f2 = b && c;
					f3 = !b && a;
					f4 = f1 || f2;
					f5 = f4 && f3;
					
					cout << " | " << a << " " << b << " " << c << " " <<   " =" << " | " << f1 << " | " << f2 << " | " << f3 << " | " << f4 << " | " << f5 << " | "  << endl;
					cout << " --------------------------------\n";
				
			}
		}
	}
	_getch();
}
